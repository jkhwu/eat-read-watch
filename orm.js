const mysql = require('mysql');

module.exports = {
    all: (tableName) => {
        // do sql shit
        return new Promise((resolve, reject) => {
            resolve(data)
        });
    },
    one: (table_name, id) => {
        // SQL SHIT that gets the data for that table yo
        return new Promise((resolve, reject) => {
            resolve(data)
        });
    }
}